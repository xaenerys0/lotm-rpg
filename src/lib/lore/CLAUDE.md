@../../../docs/rules/testing.md

# Lore Database

Structured lore data for RAG retrieval by the AI integration layer. Each entry is a semantic chunk (300-800 tokens) representing one logical entity.

> **Canon is verified against `corpus/`, never memory.** Before authoring or editing any lore entry, fact-check names, pathways, affiliations, relationships, and events against the LOTM wiki dump (`corpus/wiki/lordofthemystery_pages_current.xml`) and novel (`corpus/novel/…EPUB`). The corpus is Git LFS — if a file is a ~130-byte pointer, run `git lfs install && git lfs pull` first (`apt-get install -y git-lfs` if needed). The corpus outranks an issue's parenthetical hint. See the repo-root `CLAUDE.md` "Canon & Source Material" section.

## Structure

- `types.ts` — `LoreEntry` and `LoreCategory` type definitions.
- `retrieval.ts` — Runtime retrieval (issue #63): `retrieveChunks(query, options)`
  embeds the query with the save's **locked** embedding model (per-character
  model lock — mismatches throw) and calls the gated `match_source_chunks` RPC.
  The **timeline gate** (`canon_order <= canonPosition`) and **concealment
  gate** are enforced server-side and re-checked client-side as defense in
  depth. `createSupabaseChunkMatcher(client)` wraps a Supabase client into the
  injectable RPC seam; `retrievalChunkIds(chunks)` extracts the ids recorded on
  the turn record (determinism/debuggability); `toPgVector` serializes query
  vectors. The **epoch gate** (`RetrieveChunksOptions.epoch`, character epoch
  isolation) rides into the RPC as `p_epoch` and is re-checked client-side: a
  non-Fifth character never retrieves Fifth-Epoch canon chunks (`null` = no
  limit, eval/operator only). This **extends** the curated `lore_entries`
  injection — curated guardrails are injected first (issue #64); retrieved chunks
  fill the rest. **`concealmentTierForSequence(seq)`** maps the character's
  current sequence to the retrieval concealment ceiling (progressive spoiler
  disclosure: Seq 9-8 → 0, 7-5 → 1, 4-2 → 2, 1 → 3, 0 → 4), passed as
  `maxConcealmentTier`. **Retrieval is now wired into play**: the game loop calls
  it best-effort each turn (`components/game/lore-retrieval-client.ts` →
  `buildRetrievalQuery` from `@/lib/game`), feeding `generate({ retrievedChunks })`;
  it no-ops to `[]` when no embedding endpoint/corpus is available, so curated
  lore always stands alone as the floor.
- `selection.ts` — `selectCuratedLore(pathway, location, budget, epoch?, sequenceLevel?, excludeNpc?)`: greedy first-fit over pathway → epoch-setting → city lore, deduped, epoch-gated, and (when `sequenceLevel` is given) **sequence-gated** via `passesSequenceGate` — a pathway rung's lore is revealed only once the character has reached its earliest (highest-numbered) sequence, mirroring the glossary's `revealAtSequence` progressive disclosure. Entries with no `sequences` (geography/era/orgs) always pass. **`excludeNpc` (issue #92, canon-character takeover):** when the player IS a canon figure, any `LoreEntry` whose `npcs` includes that name (normalized via `normalizeCanonName`) is dropped — so the narrator never receives the player's OWN NPC dossier (with its forward-arc spoilers) framed as a separate present character. Pathway _mechanics_ still reach the narrator via the rules engine (`buildAICallParams`), so suppressing the prose lore costs no gameplay grounding.
- `canon-characters.ts` — **Canon-character takeover roster (issue #92).** Pure data + matcher for "play AS a novel character": `CanonCharacterPreset` (id/displayName/aliases/pathwayId/startSequence/**becomesOnScreen**/**personalityTraits**/startLocation/epoch/canonPosition/background + optional openingRecap/earlySalienceFacts) and `CANON_PLAYABLE_CHARACTERS` — the corpus-verified roster of KNOWN NOVEL FIGURES who, at their OWN introduction in the novel, are low Beyonders (Seq 7-9, "under Sequence 6") on **any of the 22 playable pathways** (NOT just ids 1-9 — all 22 are fully playable, and takeover is pathway-agnostic): **Klein Moretti** (Fool, Seq 9, Tingen), **Dunn Smith** (Darkness, Seq 7, Tingen), **Daly Simone** (Death, Seq 7, Tingen), **Leonard Mitchell** (Darkness, **Seq 8 Midnight Poet**, Tingen — corpus: he meets Klein as "Sequence 8's Midnight Poet", NOT a Seq 9 Sleepless), **Old Neil** (Hermit, Seq 9, Tingen), **Audrey Hall** (Visionary, Seq 9, Backlund), **Isengard Stanton** (White Tower, Seq 7, Backlund), **Derrick Berg** (Sun, Seq 9, City of Silver — a sealed-continent native who carries the dream-world + silver-city access flags via the optional `accessFlags` field), **Fors Wall** (Door, Seq 9, Backlund — Tarot "Magician"), **Xio Derecha** (Justiciar, Seq 9, Backlund — Tarot "Judgment"), **Trissy** (Demoness, Seq 7, Tingen — formerly the man "Tris," remade female on reaching Seq 7), **Emlyn White** (Moon, Seq 7 Vampire, Backlund — a Sanguine, born at Seq 7; Tarot "Moon"). Each preset's `startSequence`/`startLocation`/`canonPosition` reflect WHERE AND WHEN that figure is INTRODUCED (not chapter 1), and `background` is SPOILER-BOUNDED to that introduction point (durable identity/family/relationships, never the later arc). **`becomesOnScreen`** marks the figures whose novel introduction IS their becoming — they drink their first potion on-screen (**Klein**, **Audrey**, **Derrick**); the rest are already-established Beyonders, so the guided takeover prologue ends on the potion for the former and runs only up to the first appearance (no potion) for the latter. **`personalityTraits`** is the corpus-grounded disposition used to bias the choices the narrator presents toward what the figure would canonically do (seeded onto `GameState.canonPersonality`). `matchCanonCharacter(name, pathwayId)` requires BOTH a name (display name or alias, normalized) AND the exact pathway; **`matchCanonCharacterByName(name)`** matches the NAME alone (used by character creation to detect a takeover BEFORE a pathway is chosen, so the guided canon prologue can lock the fixed pathway); `getCanonCharacter(id)` is the O(1) suppression lookup; `normalizeCanonName` is the shared lower/collapse-whitespace/trim used by the matchers, the curated-lore `excludeNpc` filter, and the world-state self-suppression. The seeding helper + doppelganger suppression live in `@/lib/game/canon-takeover.ts`; the canon-faithful guided prologue generators live in `@/lib/ai/prologue-client.ts`. All canon corpus-verified.
- `sealed-artifacts.ts` — **Sealed Artifacts catalogue (corpus-verified).** The dangerous, church-catalogued mystical items (numbered by GRADE-sequential code, e.g. `0-08`, `2-049`, `3-0782`; graded 0-3 by danger — 0 ≈ Angel, cathedral-basement-only … 3 ≈ low-Sequence Beyonder). `SealedArtifact` (number/name/grade/description/**drawback**/powerEquivalence/holder?/location?/pathwayHint?/sourceRef) + the `SEALED_ARTIFACTS` const (~30 entries across all four grades, every field fact-checked against `corpus/`; the **drawback** — the "loss of control" cost — is the defining trait, never omitted). `mintArtifactItem(artifact)` produces the lightweight carried `Item` (category `"sealed-artifact"`, the code embedded in the name + the grade/drawback in the description) — the rich metadata stays out of the serialized save. `getSealedArtifact(number)`, `sealedArtifactsForGrade`, `sealedArtifactsForPathway`, `GRADE_POWER_BAND`; `sealedArtifactNumberFromItemName(name)` + `gradeForArtifactItem(item)` recover the code/grade from a minted item (the single grade resolver the combat backlash uses — `@/lib/game/combat.ts` `artifactBacklash`; now also reads the grade from a player-crafted `C<grade>-<NNN>` synthetic code via `CRAFTED_CODE_RE`/`isCraftedArtifactCode`). **Custody (Sealed Artifacts subsystem):** `ArtifactCustody` (`church`|`individual`|`unowned`) + a per-`number` `ARTIFACT_CUSTODY` table (every entry authored; dynasty/royal-line holders are `church`) drives acquisition + tradeability — `classifyHolder(holder)` derives the same from the prose and is asserted to AGREE in tests; `getArtifactCustody(number)`, `custodyForArtifactItem(item)` (a crafted `C`-code → `individual`), `ownerNameForArtifactItem(item)`, `isArtifactTradeable(item)` (everything but `church`). **Effects (the POWERS):** `ArtifactEffect {label, description, hook: EffectHook, params?}` + a per-`number` `ARTIFACT_EFFECTS` table (authored corpus-verified on ALL ~30 entries; `effectsForArtifactNumber`/`hasAuthoredEffects`); the game layer (`@/lib/game/artifact-effects`) makes them real per hook / binds them via the narrator. Acquired via `grantSealedArtifact`/`grantChurchArtifact` (`world-state.ts`), combat spoils, or **artifice** (`@/lib/game/artifice` crafting); a NON-church / crafted artifact is now market-traded + fenced (`@/lib/game/marketplace`), a church one never is. Never AI-minted (`world-state.partitionDiscoveredItems` blocks the category like a reagent). The player-facing `sealed-artifact`/`sealed-artifact-grades`/`artifice` glossary terms live in `glossary.ts`.
- `bestiary.ts` — **Combat bestiary (corpus-verified, issue #187 Phase 1).** A curated catalogue of region/Sequence-appropriate FOES the combat engine draws on so a fight never falls back to a bare "a lurking Beyonder" string. `BestiaryFoe` (id/name/`framing`/isBeyonder/pathwayId?/`sequenceBand` `[strongest,weakest]`/regions?/description/signatureAbilities/motive?/sourceRef) + `BESTIARY` (named foes corpus-verified against `corpus/` — e.g. the Devil Dog (Abyss Seq 6, Backlund), Meursault (Red Priest Seq 9), Sirius Arapis (Hanged Man Seq 9, Aurora Order), Rosago (Fool Seq 5) — plus clearly-FLAGGED generic filler — desperate thugs, a rogue Beyonder who lost control — where the corpus offers a region no named low-Seq antagonist, never invented as false canon). `getBestiaryFoe(id)`, `bestiaryFor(regionId, playerSequence)` (region + Sequence-band filter — a foe whose canon band overlaps ±2 rungs of the player), `bestiaryForPathwaySequence(pathwayId, targetSeq, regionId?)` (the ingredient-HUNT carrier filter — catalogued Beyonders of that EXACT pathway whose canon band CONTAINS `targetSeq`, region-preferred but not region-bound, so an Abyss hunter after a Seq 6-7 Characteristic finds the Devil Dog while no other pathway ever does — the alignment the engine's `deriveHuntQuarry` prefers before synthesizing a bearer), `bestiaryFoeSequence(foe, playerSequence)` (the concrete Sequence a derived enemy fights at — clamped into the canon band so the Devil Dog stays a Seq 6-7 Devil, never a Seq 9). Each entry's `framing` (an `EncounterFraming` from `@/lib/types/combat`) feeds the engine's `selectOpponents` (`@/lib/game/combat.ts`). The `sealed-artifacts.ts` data-module pattern: rich reference data here, the engine derives a lightweight `Enemy` via `bestiaryFoeToEnemy`.
- `epochs.ts` — Epoch starting points (issues #26/#29): `EPOCHS` (all five — player-safe summary, narrator `toneDirective`, the per-turn `concealment` frame + everyday `register` promoted from the prologue table for richer non-Fifth narration (issue #201, item 4; empty for the Fifth baseline), `startingLocation`, `openingBeat`, `dangerModifier`; the Fifth is the baseline for **tone** — empty `toneDirective` — but, like every other epoch, now carries an **awakening `openingBeat`** that continues from the prologue's final potion so the chronicle never opens by abruptly announcing the character's pathway), `getEpoch` (unknown → Fifth), `isFifthEpoch(epochId)` (the single shared "is this the default/Fifth era" rule — the only era with the per-city atlas/`currentCity`/travel model; reused by `@/lib/game` `place-graph.ts` and `location.ts` so they can't disagree), `epochNarrationDirective`, `epochOpeningBeat`. Threaded into prompt assembly as the `## Epoch` system layer (PromptInput.epochContext) and into the first scene's player action. **`passesEpochGate(entryEpoch, characterEpoch)`** — the epoch content gate (character epoch isolation): an **untagged** entry (`epoch` undefined/null) is **universal** and passes for everyone; a **tagged** entry passes only on **exact** epoch equality; an absent character epoch defaults to the Fifth. Mirrors `passesTimelineGate`'s "timeless rows always pass" shape and is the single rule reused by `selection.ts`, `glossary.ts`, `retrieval.ts`, and the SQL `match_source_chunks` RPC — so a non-Fifth character never sees Fifth-Epoch lore, chunks, glossary terms, or map regions (and vice versa).
- `epoch-first.ts` / `epoch-second.ts` / `epoch-third.ts` / `epoch-fourth.ts` — Rich, canon-grounded curated lore for the four pre-Iron-Age epochs (`FIRST_EPOCH_LORE` … `FOURTH_EPOCH_LORE`; grounded in the LOTM wiki/novel — Age of Chaos, Dark Epoch, Cataclysm Epoch, Epoch of the Gods). Each set's `metaphysics`-category entries are the era's **setting overview** (injected by `selectCuratedLore` via `getLoreByEpochSetting`); `location`-category entries surface in the gazetteer/discovery. Every entry is tagged with its `epoch`.
- `gazetteer.ts` — Epoch- AND city-keyed map atlas (character epoch isolation, issue #101): `gazetteerForEpoch(epoch, cityId?, accessFlags?)` returns, for the Fifth Epoch, the **current city's** districts (Tingen/Backlund/Trier/Bayam plus the start regions Pritz/Enmat/Feysac and the wider-Loen city Constant — each with its own public district list) with a header naming that city and "farther afield" travel to the OTHER Fifth cities; an unrecognised/absent `cityId` falls back to Tingen. **Continent filter (world build-out, issue #130):** `GazetteerFartherCity.continent?: "central"|"forsaken-land"` (absent = central) plus the two content-less Forsaken roster cities (`silver-city`/`giant-kings-court`, no districts — they fall back to `[]`, not Tingen) let the farther-afield list be filtered to same-continent cities, plus cross-continent ones only when the character holds the `CONTINENT_CROSSING_FLAG` (`accessFlags`, threaded from `mapAtlasFor`) — so a central character never sees the Forsaken cities in their travel panel, and vice-versa. **Crossing chokepoint + per-city flags (issues #132/#133):** `continentReachable` now gates each FORSAKEN city by its OWN `GazetteerFartherCity.requiresFlag` — Giant King's Court (`dream-world-passage`, the shared gate), the City of Silver (`silver-city-passage`), and Moon City (`moon-city-passage`) — so a Silver native sees the Court but never Moon City, a Moon native sees the Court but never Silver, and an outsider at the Court sees neither native city until admitted. A CENTRAL city shows to a mainlander, and from the Forsaken side only at the crossing city `GAZETTEER_CROSSING_CITY = "giant-kings-court"` holding the passage (leaving routes through the Court). The per-city `requiresFlag` literals + the crossing-city literal are lore-local, reconciled by a test against the game-layer `City.accessGate.requiresFlag`/`CROSSING_CITY` (the gazetteer must not import the game/AI layers), mirroring the `PROLOGUE_AFFINITY_REGIONS` pattern. Moon City carries its own district list (the fog-watch walls, the priests' hold, the lower warrens). Earlier epochs return their static, public region list (travel disabled — the CITIES model is Fifth-Epoch only). `uncertainFifthGazetteer()` (Backlund location sync) is the neutral atlas the map shows when a Fifth-Epoch city genuinely can't be resolved — no districts (so nothing is mis-pinned) but every **central** city listed as travel-reachable (the Forsaken cities stay hidden — issue #130), so setting out re-anchors `currentCity` and self-heals; the game-layer resolver `@/lib/game/location.ts` (`mapAtlasFor`) chooses it instead of defaulting to Tingen. The `cityId` is supplied by the caller (`cityIdFromLocation(location)` in `map-panel.tsx`) — the gazetteer must not import the game-layer city resolver. Player-safe public knowledge only; the narrator-only curated lore must not bleed in. Consumed by `map-panel.tsx`. **Southern Continent (world build-out 9, issue #138):** the `continent` union adds `southern-continent` and the roster adds **Balam** (the colonized Southern Continent, with its own districts) — FREELY reachable across the Berserk Sea (no flag), so `continentReachable` shows it to every mainland character (and shows the mainland back to a Balam character), while keeping it hidden from a Forsaken native except at the Court — the opposite of the flag-gated Forsaken cities. (Movement reachability — `@/lib/game/place-graph.ts`, issue #101 — keys on `cityIdFromLocation`, NOT on these district keywords, so it never false-attributes one city's sub-location to another.)
- `glossary.ts` — In-game glossary (issue #14): `GLOSSARY_TERMS` with **progressive disclosure** via `revealAtSequence` (9-8 basics at the start; rituals/organisations ~8-7; demigod concepts at 5; Sefirah-tier mysteries at 4) **and an epoch gate** (character epoch isolation) via an optional `epoch` tag. `glossaryForSequence(level, epoch?, accessFlags?, knowsMethod?)` and `sealedTermCount(level, epoch?, accessFlags?, knowsMethod?)` apply epoch, sequence, **capability AND discovery gates**; the sealed count is computed over the **epoch-, capability- and discovery-applicable** universe only, so an other-epoch, flag-gated, or undiscovered term's existence is never leaked. Fifth-specific terms (Loen, Tingen, the churches, the Saint honorific, plus the Constant/Midseashire and Intis Republic/Eternal Blazing Sun/Aurora Order terms from world build-outs 5-6) are tagged `epoch: 5`; universal mechanics (Beyonder, Sequence, Pathway, …) stay untagged; each non-Fifth epoch has its own era terms (Ancient Gods, Ancient Sun God, Solomon Empire, …). **Capability gate (issues #132/#133):** a term may carry `requiresFlag?: AccessFlag`, shown only to a character who holds that flag — so a mainland Fifth character never sees the sealed continent's vocabulary and the sealed count never hints at it. The Forsaken terms are gated PER CITY: the City of Silver / Silver Knights terms require `"silver-city-passage"`, Moon City / the Gray Fog require `"moon-city-passage"`, and the shared continent terms (Giant King's Court, Sea of Ruins, the dream-world passage) require `"dream-world-passage"` — so a Silver native never sees Moon City's vocabulary and vice versa (mutual unawareness). **Discovery gate (issue #95):** a term may carry `requiresActingMethod?: boolean` (the Acting Method term), shown only once the character has discovered the method (`actingMethodState.knowsMethod`, threaded from the panel) — and, like the capability gate, left out of the sealed universe entirely until then, so the secret's existence is never hinted before it is earned. `getGlossaryTerm(slug)`. Entries are player-safe world-building — no narrator-only lore. (World build-out 9, issue #138 adds the ungated `epoch: 5` Geography terms Southern Continent / East & West Balam / Berserk Sea, plus the Numinous Episcopate as a higher-reveal Organizations rumour.)
- `tingen.ts` — Tingen City locations and geography.
- `backlund.ts` — Backlund (Loen capital) locations: overview, Empress/Hillston/East boroughs, the Backlund Bridge & Tussock, the capital's Churches (issue #23); **deepened to capital depth (world build-out 4, issue #133)** with the borough structure (cardinal + class boroughs; Hillston = finance, St. George = industry, Empress = high society, Cherwood = the river middle), Backlund Harbor & the docklands, the underground & Tudor-Trunsoest ruins, Hillston Borough & the Stock Exchange, and the **Great Smog** (an `event` — public catastrophe with its true apotheosis-ritual cause gated). Surface locations are `epoch: 5`, `city: "backlund"`, `narratorOnly: false`.
- `trier.ts` — Trier (Intis Republic capital) locations: overview, city walls & quartiers, the Island District & Saint Viève Cathedral, the underground/catacombs, stations & transport, the Church of the Eternal Blazing Sun (issue #23); **deepened to nation depth (world build-out 6, issue #135)** with the **Intis Republic** nation overview (founded by the Sauron Family after the Trunsoest Empire fell; second-strongest Northern-Continent nation; parliamentary republic), Trier's **City-of-Fashion arts & revolutionary politics**, and the **wider Republic** beyond Trier (Lenburg/Masin, the Fog Sea Province, the Fog Sea Archipelago, the West Balam colony) — all `epoch: 5`, `city: "trier"`, `narratorOnly: false`. All canon corpus-verified. Seeded by `20260620203236`.
- `bayam.ts` — Bayam (Rorsted Archipelago capital) locations: overview, harbour/adventurers' quarter, Cathedral of Waves, streets & cemeteries, the Sea God Kalvetua & native belief (issue #23). **Deepened to archipelago depth (world build-out 8, issue #137):** added `bayam-rorsted-isles` (Blue Mountain / Symeem / Longtail isles + the colonial structure) and `bayam-sonia-sea-lanes` (the Sonia Sea's Loen/Feysac division — Gargas + Sonia Island are FEYSAC colonies — the Loen outposts Oravi/Pasu/Bansy, the Sea of Ruins, and the distant Fog Sea as a _known_ place), and a deepened `bayam-cathedral-of-waves` (the Storms diocese, Pasu-Island HQ, the Sea King Cardinal Jahn Kottman). All `epoch: 5`, `city: "bayam"`, `location`-category. **Leak fix (issue #137):** `bayam-sea-god-kalvetua` was recategorised `metaphysics → location` — a `metaphysics` + `epoch` entry is indexed as global Fifth-Epoch _setting_ (`getLoreByEpochSetting`) and injected into EVERY Fifth character's curated lore, leaking this Bayam-specific belief to the mainland; as a city-keyed `location` it stays confined to a Bayam character (the issue #132 rule that present-day region content is never `metaphysics`). Seeded by migration `20260621120354`.
- `narration.ts` — `cityNarrationDirective(location)` (issue #23): one tone sentence per city/region (Backlund = soot/class/intrigue; Trier = sun-worship/revolutionary ferment; Bayam = salt/colonial trade/island superstition; plus the farther **start regions** Pritz Harbor = naval port/Amantha frontier, Enmat Harbor = fog-bound coastal town, Feysac = God-of-Combat militarist empire, Constant = the coal-and-steel Wind City (issue #134); plus the Forsaken Land cities `silver` = the City of Silver's perpetual lightning / giant-descended people, `giant` = Giant King's Court's titan ruin and dream threshold, and `moon` = Moon City's eastern fog-watch / mutually-unaware isolation — issues #132/#133), matched on the location's lowercase first word; `null` for unknown/unmapped cities (incl. the Tingen start). Threaded into prompt assembly via `PromptInput.cityNarration`. (World build-out 9, issue #138 adds the `balam` tone — the colonized, death-haunted Southern Continent.)
- `regions.ts` — `REGIONS_LORE`: player-safe `location` overviews for the farther Fifth-Epoch start regions beyond the four hub cities (Pritz Harbor — Loen's chief naval port below the Amantha range; Enmat Harbor — a fog-bound Loen coastal town; the Feysac Empire — the God-of-Combat militarist north). Tagged `epoch: 5`, `city` = the lowercase leading word (`pritz`/`enmat`/`feysac`) so selection/narration/travel agree. Each is a first-class travel city (`travel.ts`), gazetteer entry, and glossary term. Seeded by migration `20260614020000` (Pritz's Hornacis→Amantha border correction resynced by `20260621003425`, issue #136).
- `loen.ts` — `LOEN_LORE` (world build-out 5, issue #134): the wider Loen Kingdom beyond the capital and the existing start regions. **Awwa County** (Tingen's surrounding county — keyed `city: "tingen"` so a Tingen character carries it as regional context; the Khoy/Tussock confluence, the defunct Awwa Knights' Order of Chivalry, the Awwa County Special Operations Department), **the city of Constant** (the "Wind City", Loen's coal-and-steel second city in Midseashire — a new **first-class travel city** keyed `constant`, with overview + industry/Church-of-Steam + Wind-City-coast entries), and added **depth for Pritz/Enmat Harbor** (the naval yard + storm-faith; the fog-bound smuggler's landing). All `epoch: 5`, `location`-category, `narratorOnly: false` surface geography. Constant is wired through `travel.ts` (CITIES + legs), the gazetteer (districts + farther-city), `narration.ts` (tone), `glossary.ts` (Constant/Midseashire terms), and `start-scenarios.ts`/`start-archetypes.ts` (a Constant start + Loen archetypes). All canon corpus-verified. Seeded by migration `20260620181720`.
- `feysac.ts` — `FEYSAC_LORE` (world build-out 7, issue #136): the rest of the Northern Continent's great nations, all `epoch: 5` `location`-category surface geography. **Feysac Empire deepening** (keyed `city: "feysac"`, the existing travel region): Saint Millom + Aurmir Palace + the Great Twilight Hall (`feysac-saint-millom`), and the Einhorn warrior-state — the Red-Priest imperial house, the Sonia Sea Fleet, height-as-status militarism, the Antares frontier range (`feysac-imperial-state`). Plus the **foreign nations** as lore-only city keys (NOT wired into travel/gazetteer — the off-map Stoen pattern): the **Feynapotter Kingdom** (`feynapotter`; Castiya/Justiciar royal house, Earth-Mother matriarchal breadbasket), **Lenburg** (`lenburg`; Azshara, the Knowledge-church theocracy + Holy Temple of Knowledge), and the small Knowledge splinter-states **Masin** (`masin`) and **Segar** (`segar`). The Church of the God of Combat's structure/ranks entry (`combat-church-structure`, organizations.ts) and the Feysac NPCs Awatoma/Egor Einhorn + Ozil (npcs.ts) ship alongside; a Combat-church squire and a Lenburg Knowledge-acolyte start archetype too. **Canon (corpus-verified):** the Hornacis range is the Loen–Intis border, NOT a Feysac range (Feysac↔Loen = the Amantha range; Feysac's internal range is the Antares); "Warrior"/"Hunter" are Twilight Giant sequence names, not separate pathways. Seeded by migration `20260621000738`.
- `forsaken-land.ts` — `FORSAKEN_LAND_LORE` (world build-out 3, issue #132): the sealed Eastern Continent's content. **Playable present** entries (City of Silver overview/perpetual-lightning/society, Giant King's Court overview/dream-gate, Sea of Ruins, gray-fog eastern edge) are tagged `epoch: 5` and **city-keyed** (`city: "silver" | "giant"`, the leading word of the `location` string) so `selectCuratedLore` only injects them for a character actually THERE — and being there needs the `dream-world-passage` flag (#130). None is `metaphysics`/epoch-setting (that would inject into every Fifth prompt and leak the continent to the mainland). The **Third-Epoch fall** is kept SEPARATE as one `epoch: 3` history `event` (`forsaken-land-sundering`) — never mixed on an entry (`passesEpochGate` is exact-match). Deep facts (dream gate, gray-fog/Western-Continent) are `narratorOnly` + `sequences`-gated. **Canon (corpus-verified):** the City of Silver is **giant-descended** (its people walk the **Twilight Giant** path; "Silver Knight" is an honorific for their elite, a Twilight Giant Sequence — NOT a separate faith-order), and it is one of the few surviving cities, not "the last." Giant King **Aurmir** and Giant King's Court are canon. **Moon City (issue #133):** the SECOND surviving Forsaken city — city-keyed `moon`, an eastern fog-watch descended from the Sanguines and set by the Ancient Sun God to guard the gray fog (the seam toward the sealed Western Continent), now ruled by three High Priests (Darkness/Red Priest/Justiciar) and led by **Nim**. It is gated behind its own `moon-city-passage` flag and the City of Silver behind `silver-city-passage`, so the two native populations are MUTUALLY UNAWARE at the story's start (canon — they were isolated until late). The gray-fog edge is re-keyed `silver`→`moon` (Moon City is its canon watcher). Seeded by `20260620125028`; corrected/extended by `20260620161223` and `20260620172406` (parity verified via Supabase MCP).
- `southern-continent.ts` — `SOUTHERN_CONTINENT_LORE` (world build-out 9, issue #138): the colonized lands of the old Balam Empire across the **Berserk Sea**. All present-day entries are `epoch: 5`, `location`-category, and **city-keyed `balam`** (the single present-day travel city) so curated selection injects them only for a character actually THERE — the continent overview + the Five Seas, `balam-colonies` (East Balam = Loen, West Balam = Intis), `southern-continent-tribes` (the shamanistic Death-worshipping natives), `balam-star-highlands` (the Highlands Kingdom satellite state, Reinette Tinekerr, the Paz/Haagenti lands), `berserk-sea` (the storm-wracked crossing), `balam-empire-ruins`, and the gated `death-undersea-mausoleum` (`narratorOnly` + `sequences: [4]` — the dead god's tomb beneath the sea). **Reachability (issue #138):** the Southern Continent is FREELY reachable — a long, perilous ordinary sea voyage, NOT the dream-gated Forsaken Land — so it carries no access flag (wired in `@/lib/game/travel.ts` as the `southern-continent` continent + `BERSERK_SEA_CROSSING_DAYS`, and shown in every mainland character's gazetteer travel list). **Leak control (mirrors `forsaken-land.ts`):** nothing here is `metaphysics`/epoch-setting; the Fourth-Epoch history (the Church of Death) is kept SEPARATE in `organizations.ts` as `epoch: 4` (never mixed — `passesEpochGate` is exact-match). The Balam Empire / Salinger / Azik Eggers epoch-4 history already lives in `history.ts`/`npcs.ts` (cross-linked, not duplicated). All canon corpus-verified. Seeded by `20260621133227`.
- `history.ts` — `HISTORICAL_LORE` (world build-out 12, issue #141): the named pre-Iron-Age polities, figures, and secret organizations, each tagged to **exactly one epoch (1-4)** so a Fifth-Epoch character sees none of it and vice-versa (`passesEpochGate` is exact-match; never mix epochs on one entry). The four `epoch-{first..fourth}.ts` files carry the era OVERVIEWS; this file adds the canon NAMED content. Covers the **Fourth-Epoch empires** — Solomon (Black Emperor), Trunsoest (Night Emperor), Tudor (Blood Emperor), the Tudor-Trunsoest United Empire, the **Balam Empire** (Salinger/Death — the corpus-correct fourth of the Four Emperors, which the issue list omitted), and the **Specter Empire** (corpus correction: NOT a polity but the Trunsoest Empire's legendary lost treasure-ship) — plus the **War of the Four Emperors** event; the god-emperors as NPCs (Solomon, Alista Tudor, Trunsoest, Salinger) and the **Third-Epoch** Ancient Sun God **Grisha**; and the secret organizations **Rose Redemption** (3), **Great White Brotherhood** (2), **Moses Ascetic Order**, **Secret Order**, **Blood Sanctify Sect**, and the **Demoness Family** (4). Spans epochs **2-4** — canon has NO named First-Epoch polities/figures/orgs (the Age of Chaos names only cosmological beings, already in `epoch-first.ts`). Leak-safety: empire `location`/`event` entries are public era-history (ungated); the Sequence-0 god-emperors and the SECRET organizations are `narratorOnly` + `sequences`-gated. Nothing here is `metaphysics` (so it is not auto-injected as epoch-setting) and nothing carries a Fifth-Epoch `city`/`pathway` key — historical content surfaces via RAG/discovery and can never leak across the epoch gate. All corpus-verified. Seeded by `20260620233000`.
- `start-scenarios.ts` — Varied story openings: `START_SCENARIOS` (per-epoch pool of `StartScenario` = `{id, epoch, location, blurb, openingBeat, pathwayAffinity?, origin?}`) plus the selection seam. `selectStartScenario(epoch, random?)` is a pure, deterministic-under-injected-`random` **uniform** draw (pathway does NOT bias the random draw); `selectStartScenarioForLocation(epoch, location, random?)` honours a player's **preferred** location (the scene still varies among that place's openings; falls back to a random epoch start for an unknown location). The **Fifth Epoch** is populated richly — multiple cities (Tingen/Backlund/Trier/Bayam) AND several distinct opening scenes per place, plus the farther canon regions Pritz Harbor / Enmat Harbor / Feysac. **Earlier epochs (1-4) derive their single canonical start from `EPOCHS`** (no prose drift). Each opening beat continues from the prologue's final potion and never names the pathway (mirrors the `epochs.ts` beat contract). **ORIGIN starts (issue #132):** a scenario with `origin: "forsaken-land"` (the City of Silver start) is EXCLUDED from `startScenariosForEpoch` (so the default pool, the random draw, and `startLocationsForEpoch` never land a player in an access-gated continent); `forsakenLandStartsForEpoch(epoch)` exposes them only behind the explicit "choose an origin" affordance, and `getStartScenario(id)` resolves them so the picker can pass the id through. `createDefaultGameState` seeds `accessFlags: ["dream-world-passage"]` + `currentCity` for an origin start. `startLocationsForEpoch(epoch)` aggregates the (origin-free) pool into distinct places for the picker. **Picker helpers (issue #85):** `startOptionSuitsPathway(affinity, pathwayId)` is the single "this place suits the chosen pathway" check (a suggestion only) the creation picker shares across its option labels and selected-blurb line (previously re-computed inline twice), and `describeStartLocation(option, pathwayId, pathwayName)` composes the selected-location description (blurb + optional "fitting start" suffix) — both extracted from the inline picker IIFE so the logic is pure and tested. The uniform draw now uses the shared `pickRandom` (`random.ts`). Consumed by `character-creation.tsx` → `play-dashboard.tsx` → `createDefaultGameState(..., startScenario)`.
- `start-archetypes.ts` — Start archetypes (world build-out 2, issue #131): begin a chronicle EMBEDDED in a social circle, not just at a place. `StartArchetype` = `{id, label, epoch, location, relationship, circleNpcs[], affiliationOrg?, blurb, openingBeat, pathwayAffinity?, startSequence?, minStartSequence?, openingBeatEstablished?, origin?, seeds:{trackedAllies?, society?:{orgSlug,role}, facts?}}` — **`startSequence?`** is a fixed starting sequence for born-Beyonders (e.g. `backlund-sanguine-moon` = 7; mutually exclusive with `minStartSequence`); **`minStartSequence?`** is the floor of a player-selectable sequence range (picker shown 9 down to this value; mutually exclusive with `startSequence`); **`openingBeatEstablished?`** is the opening beat used for Seq < 9 starts (an established Beyonder framing, without potion-burn language — falls back to `openingBeat` when absent). The UI layer resolves which beat to pass and which sequence the player chose, then passes `selectedSequence` to `createDefaultGameState`. — note `circleNpcs`/`affiliationOrg` are the people/org the start TIES you to, deliberately NOT called "anchors" (the Anchors system in `@/lib/game/anchors.ts` is the unrelated high-Sequence stabiliser — no conflation). The opening beat follows the scenario contract (continues from the potion, never names the pathway, ends with the scene cue). **Curated archetypes are PRESETS, never a ceiling.** **APPEND-ONLY across regions** — ships the Tingen presets, the **Backlund circle archetypes** (world build-out 4, issue #133: an attendant to Lady Audrey Hall in Empress Borough; an assistant to the detective Sherlock Moriarty in Cherwood Borough), the **wider-Loen archetypes** (world build-out 5, issue #134: a friend of the McGovern banking family in Constant City; a clerk of the Loen Relic Foundation in Stoen City — the latter opens at an off-map location, so the map shows the neutral "uncertain" atlas until first travel), the **Intis archetypes** (world build-out 6, issue #135: an acolyte of the Church of the Eternal Blazing Sun under Cardinal Plessy Descartes; an initiate of the Blazing Sun's Inquisition under Deacon Angoulême de François — both in Trier; the "assistant to the Halls" the issue mentions is already sited in Backlund as `backlund-hall-attendant`), plus (issue #132) the Forsaken **ORIGIN** archetype "A Silver Knight of the City of Silver" (`origin: "forsaken-land"`), plus the **earlier-epoch archetypes** (world build-out 12, issue #141: "A crusader of the Ancient Sun God" — epoch 3; "A legionary of the Solomon Empire" — epoch 4), each `epoch`-tagged and tied to a named historical figure from `history.ts` (a distant sovereign served, so no `trackedAllies` are seeded) and surfaced by the normal `startArchetypesForEpoch` for that era — never the Fifth. `circleNpcSuggestions` and the archetype data-integrity tests resolve names against `NPC_LORE` **plus** `HISTORICAL_LORE`. Like origin scenarios, origin archetypes are EXCLUDED from the default `startArchetypesForEpoch` and surfaced only behind the affordance via `forsakenLandArchetypesForEpoch(epoch)`; `getStartArchetype(id)` resolves them; `createDefaultGameState` seeds the dream-world passage from `archetype.origin`. **Player-authored circles (creativity uncapped):** `buildCustomArchetype(input, epoch)` turns a free-text `CustomStartCircle {tie, companions[], location?}` into a normal `StartArchetype` (trimmed + length/count-bounded), so a custom start flows through the SAME seeding pipeline — companions become tracked allies whether canon OR invented. `circleNpcSuggestions(epoch)` lists epoch-gated canon NPC names for the picker datalist (hints only). `archetypeGrounding(archetype)` is the durable, never-trimmed relationship line folded into `characterBackground` (issue #92). The GameSession-level seeding (`seedArchetype`) lives in `@/lib/game/session.ts`. **`backlund-life-school-member`** (`minStartSequence: 5`, `pathwayAffinity: [20]`): a circle-member of Will Auceptin's Life School of Thought in Hillston Borough, Backlund; the president is a Seq 2 Angel wearing a child's face. **`maxStartSequence?`** (issue #183 follow-up): the ceiling (highest/weakest selectable rung) of a `minStartSequence` range — defaults to 9 when absent, set BELOW 9 for a **born-but-climbable** archetype so the picker offers a range with a capped top (requires `minStartSequence`, mutually exclusive with `startSequence`; integrity-tested for range + `>= minStartSequence`). **`backlund-sanguine-moon`** uses it: a Sanguine is BORN a Vampire at Seq 7 (never 8/9) yet may begin already climbed, so it is now `minStartSequence: 5` + `maxStartSequence: 7` (a 7→5 picker) instead of a fixed `startSequence: 7` — its `openingBeat` serves the just-born Seq-7 default and a new `openingBeatEstablished` the climbed 6/5 (session.ts switches on `startSequence < seqCeiling`, not `< 9`). **Organization-representation archetypes (issue #183 follow-up — `ORG_REPRESENTATION_ARCHETYPES`):** four new starts tied to existing NPCs + org lore entries, broadening which organizations are playable: `tingen-psychology-alchemist` (Visionary [2], Daxter Guderian, `psychology-alchemists-overview`), `backlund-rose-temperance` (Chained [22], Sharron & Maric, `rose-school-of-thought-overview` — a Temperance exile), `bayam-storms-punisher` (Tyrant [6], Jahn Kottman, `mandated-punishers-bayam` — the orthodox storm-faith Beyonder arm, contrasting Bayam's outlawed sea-god faith), and `trier-machinery-hivemind` (Paragon [19], Horamick Haydn, `steam-church-overview`). All canon corpus-verified; the Psychology Alchemists one seeds a new NPC (Daxter Guderian) via migration `20260625180254`.
- `fifth-epoch.ts` — Fifth Epoch baseline: politics, technology, social norms (tagged `epoch: 5`).
- `organizations.ts` — Nighthawks, Mandated Punishers, Machinery Hivemind, Psychology Alchemists, Aurora Order, and (issue #132) the **Numinous Episcopate** — a Death-revival secret society originating on the Southern Continent (Eggers Family / Church-of-Death remnants), spread into the Northern Continent. Surface `overview` ungated; the `true-goal` (usurping the Evernight Goddess's dominion over death) is `narratorOnly` + `sequences`-gated. Plus the **Backlund deep-dive orgs (world build-out 4, issue #133)**: the **Rose School of Thought** and the **Tarot Club** (origins + the Fool/fate context). **Canon (corpus-verified — see root `CLAUDE.md`):** the Rose School worships the **Mother Tree of Desire** on the **Chained pathway** (an Indulgence-vs-Temperance schism, the Temperance refugees joining the Church of the Fool) — it has NOTHING to do with the Evernight Goddess (an earlier draft + the issue hint were both wrong, corrected by migration `20260620161223`). Neither the Rose School nor the Tarot Club is Backlund-LOCAL, so both carry NEITHER a `city` NOR a `pathway` key; the **Backlund Nighthawks division** IS Backlund-local (`city: "backlund"`). **Injection rule:** `selectCuratedLore` injects an org entry only when it is `city`-keyed (via `getLoreByCity`, for a character in that city) or `pathway`-keyed; an org with neither (Numinous Episcopate, Rose School, Tarot Club) is corpus/RAG + integrity only and never injected — that is the leak-control lever for cross-cutting secrets. Plus the **wider-Loen orgs (world build-out 5, issue #134)**: the **Loen Relic Search & Preservation Foundation** (founder Audrey Hall; HQ Stoen City, East Chester County) — overview ungated + its **Compliance Department** (a secret Beyonder division) `narratorOnly` + gated, both carrying NO city key (cross-cutting, never injected, like the Numinous Episcopate); the **Red Gloves** (the kingdom-wide elite Nighthawk division — no city key, roving); and the city-keyed regional **Mandated Punishers — Pritz** (`city: "pritz"`) and **Machinery Hivemind — Constant** (`city: "constant"`) presences. Plus the **Intis orgs (world build-out 6, issue #135)**: the **Church of the Eternal Blazing Sun — Trier structure & members** (`city: "trier"`, surface — Cardinal Plessy Descartes's diocese, the Inquisition's Purifiers, Saint Viève's veneration) and the gated **Aurora Order — true nature** (the True Creator's cult; **corpus canon: its pathway is the HANGED MAN, not the Sun**) which — as a cross-cutting high-concealment secret of both Loen and Intis — carries NO city/pathway key (never injected) and is `narratorOnly` + `sequences`-gated, leaving the existing `aurora-order-overview` as the ungated surface. Plus the **Orthodox Churches sweep (world build-out 10, issue #139)**: the eight Churches as `organization` entry sets — a public `*-church-overview` (ungated doctrine/structure/roster, `narratorOnly: false`, `sequences: []`) and a gated `*-church-inner-secret` (the god's TRUE NATURE + internal politics, `narratorOnly: true` + `sequences: [4]`, NO city/pathway key so it is never injected — the `aurora-order-true-nature`/Numinous-Episcopate pattern) for the Evernight Goddess, Lord of Storms, God of Steam & Machinery, God of Knowledge & Wisdom, Earth Mother (plus its Backlund branch `earth-mother-harvest-church-backlund`, `city: "backlund"`), God of Combat, and the Fool; the Church of the Eternal Blazing Sun's overview/members shipped in #135, so only its `blazing-sun-church-inner-secret` is added. Overviews are `city`-keyed to a curated travel-city seat where one exists (Storms→`bayam`, Steam→`trier`, Combat→`feysac`, Harvest Church→`backlund`) and cross-cutting (no `city`) otherwise. Plus the **Rorsted Archipelago orgs (world build-out 8, issue #137):** the **native sea-god faith** — an ungated `sea-god-faith-overview` (`city: "bayam"`, the island-superstition the colony openly knows of) plus the gated `sea-god-resistance` (the organized independence movement + the sea-god's true Tyrant-Pathway nature; `narratorOnly` + sequence-gated, NO `city`/`pathway` key so it is never injected — the `aurora-order-true-nature` pattern), and the **Mandated Punishers — Rorsted Archipelago Presence** (`city: "bayam"`, the storm-faith's Beyonder arm at its strongest on its own sea). **Canon (corpus-verified, corpus outranks the issue hint):** the formal "Church of the Sea God" is a POST-1352 Fool subsidiary, so the baseline worship is the OUTLAWED native faith + Resistance, not a church; and there is no "Sailor pathway" (the sea is the Tyrant Pathway's theme — "Sailor" is its Sequence 9). **Canon (corpus-verified — corpus outranks the issue parentheticals, root `CLAUDE.md`):** Storms governs Tyrant + White Tower (there is NO "Sailor" pathway); Earth Mother governs Mother + Moon (Demoness was only Lilith's former personal path); God of Combat governs Twilight Giant + Death + Darkness (not Warrior/Hunter/Red Priest); Evernight governs Darkness + Death + Twilight Giant. Seeded by `20260620230000`. Plus the **Southern Continent orgs (world build-out 9, issue #138):** the historical **Church of Death** (`church-of-death`, `epoch: 4`, the Balam Empire's state religion — kept SEPARATE from the present day, no Fifth-Epoch `city` key); the present-day **folk death-worship of the Balam tribes** (`southern-death-worship`, `city: "balam"`, the outlawed shamanistic faith the colonists hunt); and `numinous-episcopate-southern-roots` — a CROSS-LINK of the existing #132 Numinous Episcopate to its Balam origin + factions (Royal Family under Sia Palenque Eggers, Artificial Death under Haiter), carrying NO `city`/`pathway` key (never injected, the Numinous pattern) + `narratorOnly` + gated. The existing `numinous-episcopate-overview`/`true-goal` are NOT duplicated. Seeded by `20260621133227`. Plus the **Life School of Thought** (`life-school-of-thought-overview`): a Fifth-Epoch secret org controlling the Wheel of Fortune pathway, based in Backlund; president Will Auceptin is a Seq 2 Angel in a child's body, ally of the Tarot Club as of 1358. Cross-cutting (no `city`/`pathway` key), ungated, `narratorOnly: false`. Seeded by `20260625171913`. Plus the **Psychology Alchemists** split (issue #183 follow-up): the existing `psychology-alchemists-overview` (`pathway: "visionary"`-keyed, so injected into every Visionary character) was **de-spoilered** — its deep Twilight-Hermit-Order/Adam control moved into a new gated `psychology-alchemists-inner-secret` (the Garden of Eden + Seven-Deadly-Sins councillors, `narratorOnly: true`, `sequences: [4]`, NO pathway/city key so it is never injected — the `aurora-order-true-nature` pattern), leaving an ungated surface overview that names only the mind-science secret society and its formulas. Both support the `tingen-psychology-alchemist` start archetype. Seeded by `20260625180254`.
- `npcs.ts` — Key Tingen NPCs: Dunn Smith, Leonard Mitchell, Old Neil, Daly Simone, Azik Eggers, Klein Moretti, Melissa Moretti, Ince Zangwill; plus (issue #132) the City-of-Silver characters Derrick Berg (Tarot Club "The Sun", `city: "silver"`) and Giant King Aurmir (`city: "giant"`, `narratorOnly` + gated); plus (world build-out 4, issue #133, all corpus-verified) the Backlund figures **Audrey Hall** (the Tarot Club's "Justice", a Visionary noble of Empress Borough) with her full family (the Earl, mother **Caitlyn**, brothers **Hibbert** + **Alfred**, dog Susie), the **Hall family** house, **Klein's Backlund cover identities** (Sherlock Moriarty the Cherwood detective + Dwayne Dantès the Empress-Borough tycoon), and the **Bravehearts Bar exiles** Sharron & Maric (Rose-School Temperance refugees) — all `city: "backlund"`, `narratorOnly` for the Beyonder secrets. (Alger Wilson was REMOVED — corpus shows he is a Church-of-Storms/Sea-God Sonia-Sea figure, never Rose School or Backlund.) Like the #132 City-of-Silver NPCs, the Backlund NPCs are city-keyed but **NOT `pathway`-keyed** (the leak rule: a `pathway` field injects a regional NPC into any same-pathway character's prompt); pathways are named only in prose. Plus the **wider-Loen NPCs (world build-out 5, issue #134, corpus-verified):** **Gawain** (the last Awwa Knight, Klein's combat instructor — `city: "tingen"`), **Welch McGovern** (the Constant banker's son, Klein's Khoy classmate — `city: "constant"`), and the Loen Relic Foundation figures **Pacheco Dwayne** (Compliance deputy director, Black Emperor, `narratorOnly`) and **Barton** (an ordinary Foundation clerk with faint perception) — the Foundation pair carry NO city key (Stoen City is not a curated travel city). None is `pathway`-keyed. Plus the **Intis NPCs (world build-out 6, issue #135, corpus-verified):** **Saint Viève** (the Blazing Sun's only female angel, guardian angel of Trier and the cathedral's namesake), **Cardinal Plessy Descartes** (head of the Trier diocese), and **Angoulême de François** (a Deacon-Purifier of the Inquisition who is also a Deputy Assistant Commissioner of Trier's police) — all `city: "trier"`, Sun pathway named only in prose (NOT `pathway`-keyed), `narratorOnly` for the Angel/Saint/Beyonder truths beneath their public church roles. Plus the **Orthodox Churches sweep NPCs (world build-out 10, issue #139, corpus-verified):** the notable members of the eight Churches with relationship data — **Arianna** (Evernight, Head of the Thirteen Archbishops, uncityed), **Gaard II** (Storms Pontiff, uncityed) and **Jahn Kottman** (Storms Sea King, `city: "bayam"`), **Bornova Gustav** (Steam Guardian Angel of Trier, `city: "trier"`) and **Horamick Haydn** (Steam Archbishop / Backlund University professor, `city: "backlund"`), **Isengard Stanton** (Knowledge detective, `city: "backlund"`), **Edwina Edwards** (Knowledge Archbishop / pirate-admiral "Iceberg", uncityed) and **Brignais** (Knowledge, Savoie Mob, `city: "trier"`), **Emlyn White** + **Utravsky** (Earth Mother's Harvest Church, `city: "backlund"`), **Roland** (Earth Mother Matriarch, uncityed), **Larrion** (God of Combat Chief Shepherd, `city: "feysac"`), and **Valentine de Lacourt** (Blazing Sun Purifier, `city: "trier"`). Like the #132/#135 regional NPCs they are city-keyed where canon places them but **never `pathway`-keyed** (the leak rule — pathway named only in prose) and `narratorOnly` for the Beyonder truths. The Fool church's members Derrick Berg (#132) and Nim (#133) already exist and resolve. Plus the **Rorsted Archipelago NPCs (world build-out 8, issue #137, corpus-verified):** **Kalat** ("Baldy Kalat", the wheelchair-bound mid-Sequence leader of the independence Resistance, devotee of the outlawed Sea God), **Ralph** (the half-native Bayam trader who funds the Resistance behind a respectable ledger — the colonial-trade figure), and **Danitz Dubois** (a boatswain of Edwina Edwards's treasure-seeking Golden Dream fleet) — all `city: "bayam"`, `narratorOnly` for the Resistance/Beyonder secrets, and (the leak rule) never `pathway`-keyed. Plus the **Southern Continent NPCs (world build-out 9, issue #138, corpus-verified):** **Sia Palenque Eggers** (leader of the Numinous Episcopate's Royal Family Faction, a descendant of Salinger/Death through the Eggers line) and **Haiter** (leader of the Artificial Death Faction, once an archbishop of the Balam Empire's Church of Death) — both `city: "balam"`, `narratorOnly` + gated, never `pathway`-keyed; they cross-link to the existing Salinger (`history.ts`) and Azik Eggers entries. Plus the **secret-society & Family member NPCs (world build-out 11, issue #140, corpus-verified):** the Tarot Club members **Alger Wilson** (The Hanged Man, a founder; Tyrant→Hanged Man storms/sea figure — **re-added** in his CORRECT role after #133 removed the wrong Backlund/Rose-School attribution, so he is now uncityed + Rose-School-free), **Fors Wall** (The Magician, Door, of the Abraham line), and **Cattleya** (The Hermit, a Hermit-pathway pirate-queen); and the Family figures **Judith** (Demoness Sect matriarch, daughter of Alista Tudor + the Primordial Demoness), **Amon** (the Error "Angel of Time"), **William Augustus I** (the Augustus founder-Angel), **Antigonus** (the maggot-sealed Fool ancestor), **Camus Castiya** (Castiya/Justiciar, Feynapotter), **Elros Einhorn** (the Einhorn–Sauron dual line), **Iveljsta Eggers** (a Chained-pathway Eggers among the Fool's faithful), and **Jason Beria** (the Beria banker-Devil tied to the Twilight Hermit Order). These are the DEEPEST spoilers — all `narratorOnly` + sequence-gated `[4]`, carrying NO `city`/`pathway` key (cross-cutting — never injected); pathways/species/relations named only in prose. The org entries live in `secret-societies.ts`; existing members (Audrey Hall, Leonard Mitchell, Derrick Berg, Emlyn White, Awatoma/Egor Einhorn, Azik/Sia Eggers, Salinger) are cross-linked by name, not duplicated. Plus **Will Auceptin** (`npc-will-auceptin`): president of the Life School of Thought, Sequence 2 Angel (Wheel of Fortune), appearing as a child ~10–12 with pitch-black eyes — an ancient being from the Fourth Epoch, operating in Backlund/Hillston Borough under the alias "Will Ceres" at 3 Burningham Road; ally of the Tarot Club. `city: "backlund"`, `narratorOnly: true`, `sequences: [2]`. Seeded by `20260625171913`. Plus **Daxter Guderian** (`npc-daxter-guderian`): a doctor of the Greenhill Mental Asylum in Tingen and a junior member of the secret Psychology Alchemists — a Visionary Sequence 9 Spectator, nervous and easily leaned on, who becomes Klein's informant; the early-game, low-Sequence tie for the Psychology Alchemists start archetype. `city: "tingen"`, `narratorOnly: true`, `sequences: [9]`, never `pathway`-keyed (the leak rule). Seeded by `20260625180254`.
- `secret-societies.ts` — `SECRET_SOCIETIES_LORE` (world build-out 11, issue #140): the secret organizations & great Angel Families as `organization` entries — the social web many start-archetypes hang off. 12 entries: the **Tarot Club roster** (cross-linking the existing member NPCs by card), the **Twilight Hermit Order** (Adam's order, the Second Blasphemy Slate, resurrect the Ancient Sun God), the **Demoness Sect** (the present-day heir of the epoch-4 Demoness Family, kept separate), and the nine great Families — **Abraham** (Door, Mr. Door, now Church-of-the-Fool), **Amon** (Error; every member is Amon), **Antigonus** (Fool; destroyed by Evernight, the maggot-madness), **Augustus** (Loen royal house; Justiciar + Black Emperor), **Andariel** & **Beria** (Abyss Devil families), **Castiya** (Feynapotter royal house; Justiciar/Earth Mother), **Einhorn** (Feysac imperial house; Red Priest), and **Eggers** (Balam/Death, Salinger's line). **Leak control:** every entry is `epoch: 5`, `narratorOnly: true`, sequence-gated `[4]`, with NO `city`/`pathway` key — the DEEPEST spoilers (Tarot identities, family true natures), never curated-injected (the `*-church-inner-secret` / Numinous-Episcopate pattern). The member NPCs live in `npcs.ts`; Rose School (#133) and Aurora Order (#132/#135) already exist and are cross-linked by name, not re-authored. **Canon corrections honoured (corpus-verified):** "The World" is Klein/The Fool's own seat (not a separate person) and "Death"/Azik is a planted false rumour; Abraham is Door (founded by Mr. Door, later converted to the Fool); Andariel & Beria are Abyss Devil families (not "time"); Cattleya is Hermit-pathway (not an Andariel). Seeded by `20260621151751`.
- `pathway-fool.ts` — Fool pathway lore (Seq 9-5).
- `pathway-visionary.ts` — Visionary pathway lore (Seq 9-5).
- `pathway-sun.ts` — Sun pathway lore (Seq 9-5).
- `pathway-death.ts` — Death pathway lore (Seq 9-5).
- `pathway-darkness.ts` — Darkness pathway lore (Seq 9-5).
- `pathway-tyrant.ts` — Tyrant pathway lore (Seq 9-5).
- `pathway-door.ts` — Door pathway lore (Seq 9-5).
- `pathway-error.ts` — Error pathway lore (Seq 9-5).
- `pathway-hanged-man.ts` — Hanged Man pathway lore (Seq 9-5).
- `pathway-{white-tower,twilight-giant,justiciar,black-emperor,red-priest,demoness,mother,moon,hermit,paragon,wheel-of-fortune,abyss,chained}.ts` — The thirteen additional pathways (issue #28). Each holds a single player-safe **overview** entry naming the **full canon Seq 9 → 0 progression** (issue #99 Part A added the Saint/Angel/King-of-Angels rungs Seq 4-1 and the Sequence 0 Above-the-Sequence title — the rung names are canon, deeper per-sequence lore prose still awaits the novel source). The `pathway` field is the lowercased full name with spaces (e.g. `"white tower"`) so `selectCuratedLore` matches it.
- `region-registry.ts` — **Single source of truth for the shared scalar identity** of each Fifth-Epoch travel place (issue #85): `REGION_IDENTITIES` (`id → {name, kingdom}`) + `regionIdentity(id)` (throws on an unknown id — the drift guard). The display `name` and the bare `kingdom`/realm were authored independently in BOTH the travel layer (`@/lib/game/travel.ts` `CITIES`) and the gazetteer (`gazetteer.ts` `FIFTH_CITIES`), keyed by the same id, and drifted (travel.ts named the wrong mountain range while the lore was corrected). Now both derive name + kingdom from here; the gazetteer composes its richer `realm` as `${kingdom} — ${descriptor}` (descriptor stays gazetteer-local). Only the SCALAR identity is shared — the surface-specific PROSE (travel blurb, gazetteer descriptor + blurb, narrator tone, glossary definition, curated region lore) stays with each surface. Reconciled by tests in `region-registry.test.ts` (travel CITIES) and `gazetteer.test.ts` (farther cities).
- `random.ts` — Shared random-draw primitives (issue #85): `pickRandom(arr, random)` (draw one element, clamped at the `random()===1` boundary) and `randomIndex(length, random)` (the clamped index). The `arr[Math.floor(random()*arr.length)]` pattern was re-implemented across the game engine (`apotheosis`/`death`/`echoes`/`society`) and lore (`start-scenarios`), and only the start-scenarios copy carried the clamp; these are the single tested draw both layers now share (game→lore import). Each takes ONE `random()` sample, matching the call sites they replaced (no change to any random stream's order).
- `index.ts` — Re-exports and query helpers (`getLoreByCategory`, `getLoreByPathway`, `getLoreByEpoch`, `getLoreByEpochSetting`, etc.). `getLoreByEpochSetting(epoch)` returns the era's `metaphysics` overview entries (no city/faction) that `selectCuratedLore` injects so a character always carries its own epoch's world context regardless of the prose `startingLocation`.
- `lore.test.ts` — Data integrity tests.

> **Epoch isolation note:** pathways exist in every era, but the curated pathway lore PROSE is written with Fifth-Epoch framing (churches, Nighthawks, gaslight) and is therefore tagged `epoch: 5` — gated out of earlier epochs to avoid crossover. Pathway **mechanics** still reach every epoch through the rules engine (`buildAICallParams` passes abilities/acting from `@/lib/rules`), not this lore. The city/organization/NPC/Fifth-Epoch files are likewise tagged `epoch: 5`; the four `epoch-{first..fourth}.ts` files carry epochs 1-4.

## Database Table

`lore_entries` in Supabase (migration `20260527111842`). Seeded by `20260527113655`; the five additional pathways (Darkness, Tyrant, Door, Error, Hanged Man) are seeded by `20260612170000`; the three additional cities (Backlund, Trier, Bayam) by `20260613020000`; the thirteen remaining pathways (White Tower … Chained, overview entries) by `20260613030000`; the four pre-Iron-Age epochs (First-Fourth) by `20260614010000`; the farther start regions by `20260614020000`; the Forsaken Land (City of Silver / Giant King's Court locations + Third-Epoch history, the Numinous Episcopate, the City-of-Silver NPCs — 12 entries) by `20260620125028` (issue #132); the Backlund deep-dive (notable boroughs/landmarks, the Rose School of Thought, the capital's Nighthawks division, the Tarot Club, the Backlund NPCs — 12 entries) by `20260620152919` (issue #133); a canon-correction resync of the world-build-out lore against the corpus (Rose School → Mother Tree of Desire/Chained; Alger removed; City of Silver giant-descended; + the corrected/added Backlund entries) by `20260620161223` (issues #132/#133); a second corpus-correction resync + **Moon City** (NPC pathway fixes — Pallez→Error, Old Neil→Hermit, Ince→Death/Darkness; seven pathway-group fixes; Moon City + High Priest Nim, gated apart from the City of Silver; gray-fog re-keyed to Moon) by `20260620172406` (issues #132/#133); the **wider Loen Kingdom** (Awwa County, the city of Constant + Pritz/Enmat depth, the Loen Relic Foundation + Compliance, the regional Mandated Punishers/Machinery Hivemind/Red Gloves presences, and the NPCs Gawain/Welch McGovern/Pacheco Dwayne/Barton — 15 entries) by `20260620181720` (issue #134); the **Intis Republic** (the nation overview + Trier arts/politics + the wider Republic, the Church of the Eternal Blazing Sun's Trier members + the gated Aurora Order true-nature, and the NPCs Saint Viève/Cardinal Plessy Descartes/Angoulême de François — 8 entries) by `20260620203236` (issue #135); the **Orthodox Churches sweep** (the eight Churches' public overviews + gated inner-secrets, the Earth Mother's Backlund Harvest Church branch, and the 13 notable member NPCs — 29 entries) by `20260620230000` (issue #139); the **historical / earlier-epoch regions** (the Fourth-Epoch empires + War of the Four Emperors, the god-emperors and the Third-Epoch Ancient Sun God, and six historical secret organizations across epochs 2-4 — 18 entries, from `history.ts`) by `20260620233000` (issue #141); the **Feysac Empire & Feynapotter Kingdom** (Feysac deepening + Feynapotter + Lenburg/Masin/Segar locations, the Combat-church structure entry, and the Feysac NPCs Awatoma/Egor Einhorn + Ozil — 10 entries, from `feysac.ts`) by `20260621000738` (issue #136); a canon correction resyncing the Pritz/Feysac-frontier rows (`pritz-harbor-overview`, `pritz-harbor-naval-yard`, `mandated-punishers-pritz`) from the Hornacis range to the **Amantha** range (the true Loen–Feysac border; Hornacis is the Loen–Intis border) by `20260621003425` (issue #136); the **Rorsted Archipelago / Bayam** deepening (the isles + Sonia Sea lanes, the sea-god faith + Resistance + Mandated Punishers' maritime presence, and the NPCs Kalat/Ralph/Danitz Dubois — 8 new entries from `bayam.ts`/`organizations.ts`/`npcs.ts`, plus the recategorisation of `bayam-sea-god-kalvetua` metaphysics→location and the deepened `bayam-cathedral-of-waves`) by `20260621120354` (issue #137); the **Southern Continent** (the colonized Balam lands + tribes + Berserk Sea + Balam Empire ruins from `southern-continent.ts`, the historical Church of Death + present-day folk death-worship + the Numinous Episcopate's Southern roots from `organizations.ts`, and the NPCs Sia Palenque Eggers + Haiter from `npcs.ts` — 12 new entries) by `20260621133227` (issue #138); the **secret organizations & great Families** (the Tarot Club roster + Twilight Hermit Order + Demoness Sect, the nine Angel Families, and 11 member NPCs — 23 new entries from `secret-societies.ts`/`npcs.ts`) by `20260621151751` (issue #140); a **canon-correction resync of `npc-emlyn-white` + `npc-derrick-berg`** (corpus-verified follow-up to issue #92 — Emlyn is a Sanguine born at Seq 7 Vampire, not Seq 9, and his Master Key/Utravsky origin was inverted with a non-canon blood-bond removed; Derrick's parents are dead by the City of Silver's curse so the "living family / faith of the abandoned" framing is corrected to his lost home + the Ancient Sun God) by `20260622120000`; a **canon-correction resync of `npc-leonard-mitchell`** (corpus-verified follow-up to issue #92 — when Klein meets him Leonard is already a **Sequence 8 Midnight Poet**, not a Seq 9 Sleepless "advancing to Seq 8"; the dossier framed his past as his present) by `20260622130000`; the **Life School of Thought lore** (`life-school-of-thought-overview` org overview + `npc-will-auceptin` — 2 new entries from `organizations.ts`/`npcs.ts`) by `20260625171913`; the **organization-representation archetype lore** (issue #183 follow-up — `npc-daxter-guderian` + the gated `psychology-alchemists-inner-secret`, plus a de-spoilering UPDATE of `psychology-alchemists-overview`) by `20260625180254`.

Metadata columns for filtering: `category`, `pathway`, `epoch`, `city`, `npcs`, `sequences`, `tags`.
`embedding` column (vector 1536) is nullable — populated post-MVP via pgvector.

## Conventions

- One entry per logical concept (one NPC, one location, one sequence).
- Target 300-800 tokens per entry for retrieval precision.
- Slugs are URL-safe unique identifiers: `category-qualifier` pattern.
- TypeScript source is the canonical data; SQL seed is generated from it.
- When adding new lore, add to the appropriate `.ts` file and regenerate the seed migration.
